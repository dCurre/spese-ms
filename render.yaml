services:
  - type: web
    name: java-backend
    env: java
    build:
      commands:
        - chmod +x ./gradlew
        - ./gradlew build
    startCommand: java -jar build/libs/your-app.jar
    plan: free